[project]
name = "Back_to_human"
version = "0.1.0"
description = "Ai to Human converter"
readme = "README.md"
authors = [
  {name = "BackToHuman", email = "kasrsugeeshwara@gmail.com"}
]
maintainers = [
  {name = "BackToHuman", email = "kasrsugeeshwara@gmail.com"}
]
classifiers = [
    # TODO
]
license = {text = "MIT"}
dependencies = [
  "typer"
]
requires-python = ">= 3.10"

[project.optional-dependencies]
test = [
    "coverage",  # testing
    "pytest",  # testing
    "ruff",  # linting
    "ty", # checking types
    "ipdb", # debugging
]

[project.urls]
bugs = "https://github.com/kasrsu/back_to_human/issues"
changelog = "https://github.com/kasrsu/back_to_human/blob/master/changelog.md"
homepage = "https://github.com/kasrsu/back_to_human"

[project.scripts]
back_to_human = "back_to_human.cli:app"

[tool.ty]
# All rules are enabled as "error" by default; no need to specify unless overriding.
# Example override: relax a rule for the entire project (uncomment if needed).
# rules.TY015 = "warn"  # For invalid-argument-type, warn instead of error.

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.uv]
package = true
